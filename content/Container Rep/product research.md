- check out pricing
- need to unify cloud storage- no
	too complicated and we need to switch to kluisz bucket anyway
- public private- private for now, 
- control plane features: access controls


- concurrent limits enforce
- enforce names




### Discussion points:
1. address of the image file:
   options:
	- `kcr.io/<org>/<namespace>/<repo>/<image>:<tag>` or only 3 level hierarchy (UI considerations also)
2. UI design

## Important features
1. Add metadata to each manifest
2. Searching repositories using that metadata
## new features
1. Helm chart repository
2. Virtual repository
3. UI enhancements (things like set-me-up - on click commands, that user can copy paste to setup their system to use kcr)
4. Model repository (like private huggingface, also a quick try out feature on the UI) (google vertex ai, check out open source [modelstore](https://github.com/operatorai/modelstore))
5. 